import styled from "styled-components"

export const Card = () => {
    return (
        <CardWrapper className="br-1 bg-accent c-pointer m-3" />
    )
}

const CardWrapper = styled.div`
    width : 6rem;
    height : 6rem;
`;

